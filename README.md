## Pinky Self

```bash
> termux-setup-storage
> apt update && apt upgrade
> pkg install nodejs
> pkg install git
> pkg install bash
> git clone https://github.com/Dewagumay/dewabot.git
> cd dewabot
> bash install.sh
> npm install
> node nuy
```
